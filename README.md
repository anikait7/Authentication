# Authentication
Backend project regrading user authentication 
The user can register, log in, log out and reset their password via mail.
The project demonstrates complete authentication flow with encrypted passwords, email verification, and token-based password reset functionality.
